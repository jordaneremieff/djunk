# from pkgutil import walk_packages

# from djunk.core.testing import fixtures

# pytest_plugins = [
#     package.name
#     for package in walk_packages(fixtures.__path__, prefix=f"{fixtures.__name__}.")
# ]
